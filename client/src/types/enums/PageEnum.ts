export enum PageEnum {
  Home = '/',
  Contact = '/contact',
  NotFound = '/not-found',
  Events = '/events',
  EventsSingle = '/events/:id',
  Login = '/login',
  Register = '/register',
  Logout = '/logout',
}
